# Generative-AI-with-LSTM-Text-Generation
This project builds a text generator using LSTM networks. The model learns patterns and context from a large text dataset and predicts the next word in a sequence to create meaningful sentences. It demonstrates Generative AI, NLP preprocessing, and deep learning for automated, human-like text generation from a given seed phrase.
